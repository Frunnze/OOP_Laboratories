#include "MachineView.h"

void MachineView::brokenMachine(bool Broken)
{
    if (Broken == true)
    {
        cout << "\n- Sign: The machine is broken, wait for the repairement.";
        cout << "\n- Customer: Waits.";
    }
}